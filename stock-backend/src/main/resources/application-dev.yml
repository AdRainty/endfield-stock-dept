# 开发环境配置
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/endfield?currentSchema=public
    username: endfield
    password: endfield

  # H2 数据库配置（可选，用于纯开发测试）
  # datasource:
  #   driver-class-name: org.h2.Driver
  #   url: jdbc:h2:mem:endfield;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
  #   username: sa
  #   password:
  # h2:
  #   console:
  #     enabled: true
  #     path: /h2-console

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    root: INFO
    com.adrainty.stock: DEBUG
