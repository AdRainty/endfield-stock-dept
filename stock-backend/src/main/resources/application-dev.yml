# 开发环境配置
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:endfield}?currentSchema=public
    username: ${DB_USERNAME:endfield}
    password: ${DB_PASSWORD:endfield}

  # Redis 配置
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:123456}
      database: 0
      timeout: 5000ms

  # Liquibase 日志
  liquibase:
    enabled: true

  # Spring AI 配置
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:sk-your-api-key}
      chat:
        options:
          model: ${OPENAI_MODEL:gpt-4o-mini}
          temperature: 0.7
          max-tokens: 2000
      base-url: ${OPENAI_BASE_URL:https://api.openai.com/v1}

mybatis-plus:
  configuration:
    # 开发环境开启 SQL 日志
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl

# 微信公众号配置（用于扫码登录）
wechat:
  mp:
    app-id: ${WECHAT_MP_APP_ID:wxcde29822ab2e6740}
    app-secret: ${WECHAT_MP_APP_SECRET:42388141e369e00657b88b433a207b4b}
    token: ${WECHAT_MP_TOKEN:adraintyToken871837050}
    aes-key: ${WECHAT_MP_AES_KEY:YGsaMqBjh0rakedhw72wZ8Q0zEbiJybkhwQGQGIBAli}

logging:
  level:
    root: INFO
    liquibase: INFO
    me.chanjar.weixin: DEBUG
    org.springframework.ai: DEBUG
